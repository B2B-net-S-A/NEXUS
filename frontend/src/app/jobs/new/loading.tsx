import { NewJobPageSkeleton } from "@/components/v2/jobs/new/NewJobPageSkeleton";

/** Miękka nawigacja z listy („Nowa rekrutacja”) — szkielet od razu, nie pusty obszar. */
export default function NewJobLoading() {
  return <NewJobPageSkeleton />;
}
