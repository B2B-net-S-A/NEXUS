[
 {
  "id": "SEARCH-P0-03",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "Filter and scoring still read different skill sources. structured_candidate_search.py:49-65 — _skills_text() = cast(Candidate.skills)+cast(Candidate.tags); _skill_match() does a bare substring ILIKE (`%skill%`, no word boundary → 'Go' matches 'Django'/'MongoDB'/'Golang'). These chips are AND-ed into the hard WHERE at api/search.py:280 (build_structured_filter). Meanwhile scoring_service.py:612-635 candidate_skill_names() cascades skills+verified_tech → cv_extracted_data.traffit_technologie → raw_cv_text → tags (alias-canonicalized), and its docstring notes ~99% of imported candidates have an EMPTY structured `skills` column — so a skills_must chip hard-excludes exactly the candidates scoring rates highly. grep for verified_tech|traffit_technologie|candidate_skill_names|canonical_skill across the whole filter path (structured_candidate_search.py, advanced_candidate_search.py, api/search.py, schemas/candidate_search.py) = 0 matches. PR #731 (the Phase 1 containment touching this file) fixed only P0-01/P0-02/P1-01/P2-01, not P0-03. Skill chips still live in schema (candidate_search.py:57-59: skills_must/any/none).",
  "fix_hint": "Make the skills chip filter read the same canonical, multi-source skill set the scorer uses instead of a substring ILIKE over skills+tags: either (a) expand _skills_text() to also cover verified_tech + cv_extracted_data->>'traffit_technologie' + raw_cv_text and match against alias-resolved canonical tokens with word boundaries (not raw substrings), or (b) demote skill chips from a hard WHERE-AND to a soft scoring signal so a high-scoring candidate is never cut pre-ranking. Add a golden test asserting a verified_tech-only 'Python' candidate survives skills_must=['Python'] and that 'Go' does not match 'Django'.",
  "confirm": "No guard found; divergence is live. FILTER path: structured_candidate_search.py:49-65 — _skills_text() reads only cast(Candidate.skills)+cast(Candidate.tags) and _skill_match() does a bare \"%skill%\" ILIKE (line 64, no word boundary; confirmed no regexp/~*/\\y guard in the file). These chips are unconditionally AND-ed into the hard WHERE at api/search.py:280 (build_structured_filter), and the endpoint is live (main.py:710, POST /api/search/candidates). SCORING path: scoring_service.py:612-635 candidate_skill_names() cascades skills+verified_tech → cv_extracted_data.traffit_technologie → raw_cv_text → tags (canonicalized); its live docstring (lines 615-617) still says ~99% of imported candidates have an EMPTY structured skills column, so a skills_must chip hard-excludes the very candidates scoring rates highly. grep of the entire filter path (structured_candidate_search.py, advanced_candidate_search.py, api/search.py, schemas/candidate_search.py) for verified_tech|traffit_technologie|candidate_skill_names|canonical_skill = 0 matches. Ruled out other guards: v3 schema (candidate_search_v3.py) has no separate executor — to_legacy funnels back through build_structured_filter/_skill_match; alternate GET /api/candidates path (candidates.py:752-786) is a different endpoint and still omits cv_extracted/raw_cv; no canonical/materialized skills column; the two candidate.skills= writes (cv_enrichment.py:114, m365/attachment_handler.py:264) are per-CV-parse, not a bulk backfill of the imported ~99%; git log on the file (#743/#739/#731) shows #731 Phase-1 containment never touched _skill_match/_skills_text."
 },
 {
  "id": "AI-P0-01",
  "verdict": "PARTIAL",
  "effort": "M",
  "evidence": "PR #729 (plan PR1) fixed one of the three named defects — the ablation monkeypatch no-op. Eval now passes weights explicitly: eval_matching.py:159-173 `_to_scoring_profile` → rank_candidates_for_job(..., profile=) (line 382-388), so ablations differentiate, and the flow mirrors production recommendations.py:187-197 (_build_job_text → search_candidates_semantic → rank_candidates_for_job). BUT two named gaps remain OPEN: (1) DATA LEAKAGE — scoring_service.py:889-898 `_score_champion_fit` reads CandidateStage for the exact (candidate, current job) with screening_answers IS NOT NULL and awards up to 10/100 pts; those answers exist only for candidates advanced to screening, which is precisely the ground-truth positive set built in eval_matching.py:315-320 (`stage in POSITIVE_STAGES`, screening>=0.5). The eval runs the default profile (champion_fit=10) with no temporal holdout / no feature masking / no zeroing (rank_candidates_for_job line 1058-1077 has no eval-mode guard), so metrics are inflated by label leakage. (2) NOT A QUALITY GATE — _run returns 0 regardless of metric levels (eval_matching.py:813); _go_no_go (493-536) is advisory markdown with no threshold-based non-zero exit; CI only runs tests/test_eval_matching.py (ci.yml:100, unit tests of helper math), never the eval against a fixed dataset (it needs live DB + Voyage + historical pipeline).",
  "fix_hint": "Close leakage: add an eval-mode that either zeroes pipeline-derived layers (pass a profile with champion_fit=0, or a flag that skips _score_champion_fit) or applies a temporal cutoff so stages/screening_answers created at-or-after the prediction point are hidden from both ground truth and scoring. Then make it a gate: add --fail-under thresholds (e.g. min Recall@20 / Precision@5) that make main() return non-zero, snapshot a fixed reproducible eval dataset, and wire an actual eval run (not just the unit test) into a CI/nightly job.",
  "confirm": "PARTIAL confirmed. Only 1 of 3 named defects is fixed; 2 remain with no guard. (1) FIXED — ablation monkeypatch no-op closed by PR #729: eval_matching.py:159-173 (_to_scoring_profile) passes weights explicitly into rank_candidates_for_job(..., profile=) at line 382-388. (2) DATA LEAKAGE still OPEN — scoring_service.py:889-898 (_score_champion_fit) reads CandidateStage.screening_answers for the exact (candidate.id, job.id) and awards up to champion_fit points (default 10, eval_matching.py:144). Those answers exist only for candidates advanced to screening, which is precisely the ground-truth positive set built at eval_matching.py:315-320. The default/primary eval run and go/no-go verdict use DEFAULT_PROFILE(champion_fit=10). No eval-mode guard: score_candidate_job:1003 calls _score_champion_fit unconditionally, rank_candidates_for_job:1058-1077 has no masking/holdout parameter, and repo-wide grep for eval_mode|holdout|feature_mask|temporal|as_of returns nothing in the scoring/eval path. (3) NOT A QUALITY GATE still OPEN — eval_matching.py:_run returns 0 regardless of metrics (line 813); only non-zero exit is return 2 when no jobs found (line 714); _go_no_go (493-536) is advisory markdown. CI (ci.yml:100) runs only tests/test_eval_matching.py, a 103-line DB-free helper-math unit suite, never the eval against a fixed dataset; no workflow/Makefile/wrapper invokes scripts.eval_matching as a gate. Genuine search across shared deps, middleware, decorators, entrypoint mirrors, DB constraints, git history, and all .github/workflows found no guard closing defects 2 or 3."
 },
 {
  "id": "AI-P0-04",
  "verdict": "PARTIAL",
  "effort": "M",
  "evidence": "Durable outbox exists (PR #740): model backend/app/models/index_outbox.py, service backend/app/services/index_outbox_service.py (enqueue in caller txn, compare-and-set worker, desired_hash/indexed_hash freshness). But re-embed/enqueue is wired into ONLY three candidate paths: candidates.py:3828 (create_from_cv), candidates.py:3964 (upload_cv), public_share.py:543 (invite-apply inline embed). The profile-edit path update_candidate (PATCH /{candidate_id}, candidates.py:3498-3547) mutates embedding-text fields (skills, verified_tech, tags, experience, preferences, ai_summary, competence_category) and even invalidates the score cache at 3531-3534 but NEVER calls schedule_or_embed_candidate → stale vector. The Traffit importer app/services/traffit/importer.py commits candidate content in 20+ places (daily 49k delta sync) with ZERO embed/reindex calls; cv_backfill.py (name/CV re-parse) likewise. No global backstop: only ORM event listener is on Contract (contract.py:514), none on Candidate; candidate.py has no reindex hook. Both outbox flags default False (config.py:71-72: AI_INDEX_OUTBOX_ENABLED, AI_INDEX_WORKER_ENABLED), so nothing drains a queue the edit/import paths never fill.",
  "fix_hint": "Call schedule_or_embed_candidate(candidate.id, db) from update_candidate (PATCH, after flush, when any embedding-text field changed) and from the Traffit importer's candidate upsert path plus cv_backfill name enrichment; then enable AI_INDEX_OUTBOX_ENABLED + AI_INDEX_WORKER_ENABLED so the durable queue actually backstops any missed path. Optionally add an after_flush ORM hook on Candidate to enqueue on any content change so no future write path can silently skip reindex.",
  "confirm": "No guard exists — finding confirmed still open. Re-embed/enqueue is wired into only 3 candidate write paths (candidates.py:3828 create_from_cv, candidates.py:3964 upload_cv, public_share.py:543 invite-apply inline embed). The profile-edit path update_candidate (PATCH /{candidate_id}, candidates.py:3498-3547) mutates embedding-text fields (skills, verified_tech, experience, tags, competence_category, years_it_experience, ai_summary — all in CandidateUpdate schemas/candidate.py:122, all consumed by embedding_service._build_candidate_text_v1) and invalidates the score cache (candidates.py:3531-3534) but NEVER calls schedule_or_embed_candidate/embed_candidate/enqueue → stale vector. traffit/importer.py and cv_backfill.py contain zero embed/reindex references. No backstop: the only ORM event listener is on Contract (contract.py:514-515), none on Candidate; the sole index background loop (index_outbox_loop, main.py:534) only drains an existing queue — claim_batch SELECTs solely from IndexOutboxEvent (index_outbox_service.py:191-195) and never scans Candidate for content drift, so a mutation never enqueued is never reconciled. No lazy re-embed at query/match time. Both flags default False (config.py:71-72). Verified via full grep of embed/reindex callsites, ORM listeners, background loops, and git log (outbox added in #740, nothing wired since)."
 },
 {
  "id": "AI-P0-06",
  "verdict": "PARTIAL",
  "effort": "M",
  "evidence": "One of the four missing dimensions was closed. match_score.py:54-56 adds a `scoring_algorithm_version` column; match_score_cache.py:138 (single) and :183-184 (bulk) only treat a row as fresh when `row.scoring_algorithm_version == SCORING_ALGORITHM_VERSION`, and scoring_service.py:171-175 derives that string from the AI_SCORING_CONTRACT_V2 flag — so a formula/contract change auto-invalidates. Candidate field edits also invalidate via explicit mark_stale_for_candidate (candidates.py:3531-3534 for {skills,verified_tech,tags,salary_expectation,availability_date,preferences,location,status}; :3648 after CV enrichment). But the key is still only (candidate_id, job_id, profile_id) (match_score.py:30-32) plus that single binary version, and real gaps remain: (1) WEIGHTS — scoring_weights.py:135-154 update_profile rewrites row.weights=... in place with no mark_stale and no version bump (SCORING_ALGORITHM_VERSION depends only on the v2 flag, not weights), so cached rows for that profile_id keep serving old-weight scores — the finding's 'po zmianie wag' reproduces; (2) RE-EMBEDDING/INDEX — no embedding/index path (embedding_service.py, index_outbox_service.py, index_outbox_worker.py, backfill_planner.py) calls mark_stale_*, so a bulk re-embed or model change leaves the cached semantic layer stale — the finding's 'po ponownym zaembedowaniu kandydata' reproduces; (3) TAXONOMY — no taxonomy_version/embedding_version/source_hash in the key or freshness check, and skill-taxonomy edits invalidate nothing.",
  "fix_hint": "Fold the remaining dimensions into the freshness stamp or add explicit invalidation: (a) include a hash of the active profile's weights (or have scoring_weights.py update_profile/delete_profile mark_stale all rows for that profile_id); (b) call mark_stale_for_candidate (or mark_stale_for_many_candidates) from the index_outbox re-embed worker after re-indexing; (c) add a taxonomy/embedding-model version component to SCORING_ALGORITHM_VERSION so taxonomy or model bumps invalidate the cache.",
  "confirm": "No guard closes the remaining dimensions after a wide search. Cache key is only (candidate_id, job_id, profile_id) (match_score.py:30-32) and both freshness paths check only `stale` + `scoring_algorithm_version` (match_score_cache.py:135-139 single, :182-184 bulk). That version derives solely from the AI_SCORING_CONTRACT_V2 flag (scoring_service.py:171-175). (1) WEIGHTS: update_profile overwrites row.weights in place (scoring_weights.py:147-152) with NO mark_stale, no version bump; there is no scored_at-vs-profile.updated_at comparison (scored_at only written at match_score_cache.py:103), and ScoringWeightProfile.updated_at (scoring_weight_profile.py:42-44) has no onupdate and isn't set by the endpoint — so editing a profile's weights keeps serving old-weight cached scores. (2) RE-EMBEDDING/INDEX: no embedding/index/backfill service calls mark_stale (verified empty in embedding_service.py, index_outbox_service.py, backfill_planner.py, process_backfill.py); index_version is a hardcoded \"index-legacy-v1\" (matching_contracts.py:46) used only for telemetry, not cache freshness. (3) TAXONOMY: taxonomy_version is a hardcoded \"taxonomy-legacy-v1\" audit stamp (matching_contracts.py:48; match_telemetry_service.py) absent from the cache key and freshness check. Only mark_stale callers are candidate field edits (candidates.py:3534), CV enrichment (candidates.py:3648), pipeline stage (pipeline.py:1142), job edits (jobs.py:1009) — none cover weights, re-embedding, or taxonomy. Finding remains PARTIAL."
 },
 {
  "id": "M3-CACHE-01",
  "verdict": "PARTIAL",
  "effort": "S",
  "evidence": "Primary path FIXED by #776 (534cefc1): bulk_get_or_compute gained an allow_cache_write guard — backend/app/services/match_score_cache.py:206 `if pending_writes and allow_cache_write:` skips persisting degraded composites. Both bulk callers detect the outage and disable writes: recommendations.py:267 and compute_proposals.py:179 pass `allow_cache_write=not semantic_degraded`, where semantic_degraded = not candidate_ids (recommendations.py:206, compute_proposals.py:142). search_candidates_semantic returns [] (not raise) when Voyage/Qdrant fail (embedding_service.py:463-468, 491-494), so an outage yields an empty pool → semantic_degraded=True → no fresh write. The pipeline-scores path (recommendations.py:431) omits the flag but is safe: it only fresh-computes ids present in similarity_map, which is empty during an outage. RESIDUAL GAP: the single-pair get_cached_or_compute (match_score_cache.py:145-151) still writes stale=False UNCONDITIONALLY, no allow_cache_write guard. Its caller match_justification_service._compute_breakdown (match_justification_service.py:314-327) catches a Qdrant/Voyage outage in `except Exception` at line 322, leaves similarity=None, then calls get_cached_or_compute at line 325 — persisting a neutral-semantic (degraded) breakdown as fresh into the same shared (candidate, job, DEFAULT_PROFILE) cache that /recommendations reads. Opening the Dopasowanie justification tab during an outage reproduces M3-CACHE-01.",
  "fix_hint": "Add an allow_cache_write param to get_cached_or_compute (mirroring bulk_get_or_compute), and in match_justification_service._compute_breakdown distinguish a genuine provider outage (the similarity_for_candidate_ids call raised) from a candidate that simply has no embedding, passing allow_cache_write=False only on outage so the degraded neutral-semantic breakdown is returned for display but not persisted as fresh.",
  "confirm": "No guard closes the single-pair path. get_cached_or_compute (match_score_cache.py:114-151) has no allow_cache_write parameter and persists stale=False unconditionally via _upsert_breakdown (line 146 → lines 92/104). The #776 fix (534cefc1) added the guard ONLY to bulk_get_or_compute (match_score_cache.py:206) — verified in the commit diff; the single-pair function and its caller were untouched, and the commit message scopes M3-CACHE-01 to \"/recommendations i generatora proposal snapshots\" only. During a Voyage/Qdrant outage, match_justification_service._compute_breakdown (match_justification_service.py:314-327) gets similarity=None (embedding_service.py:515-519 and :535-539 both return {} instead of raising), then calls get_cached_or_compute(semantic_similarity=None) with no write guard; score_semantic(None) → points=0.0 \"brak embeddingu\" (scoring_service.py:974-975), and score_candidate_job never re-fetches, so a degraded breakdown is persisted stale=False. It runs before the quota gate (get_or_generate line 419 vs _gate_and_count line 434), so every Dopasowanie-tab open during an outage can poison the shared DEFAULT_PROFILE (id=0) cache, which /recommendations reads via bulk_get_or_compute stale.is_(False) rows (recommendations.py:261-268 with resolve_active_profile → DEFAULT_PROFILE in default prod config). The route candidate_scoring.py:79-112 gates only RBAC (OperationalUser) + rate limit — no AI-health/degraded check; post-#776 commit #787 only removed a PEP-563 import. No middleware, DB constraint, or entrypoint mirror covers cache-write semantics."
 },
 {
  "id": "M3-TX-01",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "All three named services still commit/rollback the CALLER's request session (no dedicated session, no savepoint). match_score_cache.py:147/150 (get_cached_or_compute) and :210/213 (bulk_get_or_compute) — `db` is the request session Depends(get_db) from recommendations.py:261. match_telemetry_service.py:158/161 (record_impressions) and :209/213 (record_outcome). index_outbox_service.py: schedule_or_embed_candidate/job (lines 139,170) call _commit_enqueue (174-180) which does db.commit()/db.rollback() on the caller session. Concrete write-path repro: candidates.py:3820 `await db.flush()` (uncommitted Candidate+Activity+UserActivity) then :3828 schedule_or_embed_candidate → with AI_INDEX_OUTBOX_ENABLED on, _commit_enqueue commits the in-flight business TX early and on error rolls the candidate creation back (before the intended commit at 3834). Only mark_stale_for_* was changed to defer to the caller (partial cleanup); grep shows no begin_nested/savepoint in the three files, and the sole AsyncSessionLocal is in the worker drain path (index_outbox_service.py:217), not the hot-path writers.",
  "fix_hint": "Make the auxiliary writers stop owning the caller's transaction: either flush-only and let the endpoint's own db.commit() persist them (like mark_stale_* already does), or perform the best-effort write on a dedicated AsyncSessionLocal() session (mirroring _default_reindex at index_outbox_service.py:217) so a telemetry/cache/enqueue failure can never commit or rollback the business operation. Concretely, drop db.commit()/db.rollback() from get_cached_or_compute, bulk_get_or_compute, record_impressions, record_outcome, and _commit_enqueue.",
  "confirm": "No guard exists. All three services still commit/rollback the CALLER's request session with no dedicated session and no savepoint: match_score_cache.py:147/150 (get_cached_or_compute) and 210/213 (bulk_get_or_compute) — not even flag-gated; match_telemetry_service.py:158/161 (record_impressions) and 209/213 (record_outcome); index_outbox_service.py:177/180 (_commit_enqueue), invoked from schedule_or_embed_candidate/_job at 139/170. grep confirms no begin_nested/savepoint in any of the three files (savepoints exist only in unrelated services: notification_triggers.py:89, kpi_coach_service.py, cortex extractors, autenti); the sole AsyncSessionLocal is in the worker drain path index_outbox_service.py:217, not the hot-path writers. Only mark_stale_for_* (219/229/239) was fixed to defer to the caller (partial cleanup). get_db merely yields the request session — no middleware/decorator wraps these in a nested TX. The write-path repro at candidates.py:3820→3828 is real and flag-independent: the early commit happens with outbox ON via _commit_enqueue AND with outbox OFF (default) via the embed_candidate fallback (index_outbox_service.py:116 → embedding_service.py:409 db.commit()), so the intended bounding commit at candidates.py:3834 no longer owns the transaction and a failure in _auto_assign_primary_cc (3832) can no longer roll back the candidate creation. Feature-flag defaults of False are not a guard. Finding is genuinely still open."
 },
 {
  "id": "M3-ACT-01",
  "verdict": "PARTIAL",
  "effort": "M",
  "evidence": "The three UI entry points named in the finding were consolidated onto one endpoint that does snapshot: SuggestedJobsWidget, SuggestedCandidatesWidget and QuickAssignV2.tsx all call recommendationsApi.assignToJob → POST /api/candidates/{id}/assign-to-job/{job_id} (frontend/src/lib/api.ts:2560), whose handler assign_candidate_to_job (backend/app/api/recommendations.py:975-1078) creates the CandidateStage then does db.flush() + create_original_cv_snapshot(db, stage) at recommendations.py:1076-1077. Pipeline moves (app/api/pipeline.py:592/1685/1820) and invite-apply (app/api/public_share.py:473) also snapshot. BUT two genuine 'assign candidate to job' paths still create CandidateStage rows WITHOUT the snapshot (and without its snapshot_created Activity audit): (1) bulk_add_proposals — route POST /jobs/{job_id}/proposals/bulk, the manual-search bulk-add the candidates_bulk dispatcher delegates to — creates the stage at app/api/proposals_bulk.py:327-334 and commits at :364 with no create_original_cv_snapshot call; (2) promote_shortlist_entry — route POST /shortlist/{entry_id}/promote — creates the stage at app/api/job_shortlist.py:298-305 and commits at :308 with no snapshot call. The shared service's own docstring (app/services/candidate_stage_cv_service.py:3-6) asserts the invariant 'every CandidateStage must have exactly 1 candidate_stage_cvs entry' and lists the snapshotting callers — bulk and shortlist are absent. The reader _load_csv_for_stage (app/api/candidate_stage_cv.py:105-118) 404s ('CV instance nie istnieje dla tego stage. Snapshot powinien być utworzony przy CREATE stage'a') for any stage missing a snapshot. So the finding still reproduces: whether an assignment records an original-CV snapshot + audit Activity depends on which button was clicked.",
  "fix_hint": "Route every stage-creating path through one shared helper (e.g. a create_pipeline_stage() that adds the stage, flushes, and calls create_original_cv_snapshot). Concretely: in proposals_bulk.bulk_add_proposals, after db.add(stage) do await db.flush() then await create_original_cv_snapshot(db, stage) per added candidate before the final commit; and in job_shortlist.promote_shortlist_entry, add await db.flush(); await create_original_cv_snapshot(db, stage) before db.commit(). That restores the documented invariant (one CandidateStageCV + one snapshot_created audit Activity per stage) regardless of entry point.",
  "confirm": "No guard exists — finding confirmed still open (and broader than stated). Searched all guard classes: (1) ORM events — grep listens_for/event.listen in backend/app/ yields only Contract (contract.py:514-515); recruitment_pipeline.py:92 CandidateStage model has NO before_insert/after_insert hook, so stage creation never auto-snapshots. (2) DB trigger — migration 0069_candidate_stage_cv.py creates only the table + UNIQUE(candidate_stage_id) (dedup, not autocreate); 0070 is a one-shot backfill, no CREATE TRIGGER on candidate_stage_cvs. (3) Lazy reader — _load_csv_for_stage (candidate_stage_cv.py:105-121) hard-404s (\"uruchom backfill 0070\"), never self-heals. (4) Dispatcher — candidates_bulk.py:145-149 just delegates the write to POST /jobs/{id}/proposals/bulk, no snapshot. Only 4 files call create_original_cv_snapshot: pipeline.py:592/1685/1820, recommendations.py:1077, public_share.py:473. Enumerating every CandidateStage( in backend/app/, FIVE paths create a stage with no snapshot + no snapshot_created Activity: proposals_bulk.py:327 (commit :364), job_shortlist.py:298 (commit :308), jobs.py:2336, candidates.py:2304, services/signing/pipeline_hook.py:88. Git log shows shortlist/bulk features (#749/#754/#755) postdate the snapshot feature (f8077095) with no later fix. Whether an assignment records an original-CV snapshot + audit still depends on which button was clicked."
 },
 {
  "id": "M4-P0.2",
  "verdict": "PARTIAL",
  "effort": "M",
  "evidence": "The live system is the legacy PipelineTemplate/PipelineStageDef path (pipeline_templates.router mounted at main.py:888; Job.pipeline_template_id → pipeline.py move_candidate → auto-draft-Contract).\n\nCORE IMPACT STILL REPRODUCES — a custom \"Zatrudniony\" stage does not create a contract:\n- pipeline.py:394-400 derives legacy_enum = data.stage or PipelineStage.new, only overridden when stage_def.legacy_enum_value is set.\n- A custom stage cannot carry legacy_enum_value: StageDefCreate (schemas/pipeline_template.py:14-24) has no such field, and clone_template deliberately doesn't copy it (pipeline_templates.py:348). So a custom hired stage has legacy_enum_value=None, is_terminal=True, terminal_type=hired.\n- pipeline.py:695-720 auto-creates the draft Contract ONLY `if legacy_enum == PipelineStage.hired`. terminal_type is fully known at that point but is never used to derive the hire signal. Grep confirms NO TerminalType.hired→PipelineStage.hired mapping anywhere in pipeline.py's move/contract path. Result: moving a candidate to a custom hired stage yields legacy_enum=new → no draft Contract (exactly the finding's impact). list_stages (pipeline.py:266-269) even returns stage=PipelineStage.new for custom stages, so the FE can't supply `hired` either.\n- The only hire-forcing that exists is signing/pipeline_hook.py:84-85, but it fires only in the contract-signing flow and matches the literal string name \"Zatrudniony\" — it does NOT cover the manual Kanban /move path or the auto-contract trigger, and misses any differently-named custom hired stage.\n\nMITIGATIONS THAT DID LAND (why PARTIAL not OPEN):\n- clone_template now copies stages AND rejection reasons (pipeline_templates.py:333-364) — the finding's \"klonowanie nie kopiuje reguł odrzuceń\" is addressed.\n- terminal_type/is_terminal consistency now enforced on create/update (add_stage 393-404, update_stage 506-517).\n- A new parallel semantic registry exists (semantic_states.py + workflow_revision StageRevision.semantic_key + admin_workflows.py create_draft_from_revision with full clone parity incl. scorecard_schema). But this registry is NOT wired into the live move/contract/KPI logic — hire detection at ~20 sites (reports.py, candidates.py, jobs.py, competitions.py, dashboard_metrics.py, pipeline.py:698) still keys strictly off CandidateStage.stage == PipelineStage.hired.\n\nREMAINING GAPS: (1) custom terminal_type=hired stage does not trigger the auto-draft Contract — no terminal_type→legacy-hired mapping in /move; (2) legacy clone_template still omits scorecard_schema (pipeline_templates.py:336-350 copies name/order/category/tracker/sla/terminal_type but not scorecard_schema).",
  "fix_hint": "In pipeline.py move_candidate, before the hire/terminal logic, map terminal semantics off the stage_def when legacy_enum_value is absent: `if stage_def and stage_def.is_terminal and stage_def.terminal_type == TerminalType.hired and legacy_enum is not PipelineStage.hired: legacy_enum = PipelineStage.hired` (and symmetrically for rejected/withdrawn). This makes pipeline.py:698 auto-create the draft Contract for custom hired stages. Also copy scorecard_schema in clone_template (pipeline_templates.py:336-350).",
  "confirm": "STILL OPEN — genuine wide search found NO guard closing the core impact (custom terminal_type=hired stage → no auto-draft Contract). The live /move endpoint is move_candidate (pipeline.py:305, mounted main.py:681). Hire signal derivation at pipeline.py:394-400 sets `legacy_enum = data.stage or PipelineStage.new` and only overrides it when `stage_def.legacy_enum_value` is truthy — there is NO terminal_type→hired fallback. The auto-draft Contract+ClientOrder block at pipeline.py:698 gates strictly on `legacy_enum == PipelineStage.hired`; the stage's terminal knowledge is computed at pipeline.py:405-408 (`is_terminal_target`) but used ONLY for a capability guard, never to derive the hire signal. A custom hired stage cannot carry the mapping: StageDefCreate/StageDefUpdate (schemas/pipeline_template.py:14-24 / 27-38) have no `legacy_enum_value` field, add_stage does `PipelineStageDef(**data.model_dump())` (pipeline_templates.py:405) so legacy_enum_value stays None, and clone_template (pipeline_templates.py:333-348) explicitly comments out legacy_enum_value AND omits scorecard_schema (grep: scorecard_schema NONE in pipeline_templates.py — confirms remaining gap #2). list_stages (pipeline.py:258-269) returns `stage=PipelineStage.new` fallback for custom stages, so the FE cannot supply `hired` either. The only hire-forcing that exists (signing/pipeline_hook.py:82-85) fires solely for the literal string \"Zatrudniony\" inside the contract-signing flow (direct CandidateStage insert), NOT the manual Kanban /move path, and misses any differently-named custom hired stage. The parallel semantic registry (semantic_states.py / workflow_registry_service.py) is NOT wired into the live path — zero `semantic` references in pipeline.py; it is imported only by admin_workflows.py and process_backfill.py. No DB CHECK/trigger/entrypoint mirror forces legacy_enum_value on hired terminal stages. Corroborating: the codebase ships a read-only diagnostic admin_pipeline_inventory.py:182-193 (`custom_stage_legacy_new`, P1) that DETECTS exactly this symptom (\"custom stage_def bez legacy_enum_value, zapisany jako legacy 'new' — semantyka etapu zlana do 'new'\"), i.e. measure-first acknowledgement, not a fix. Mitigations that landed (clone now copies stages+rejection reasons pipeline_templates.py:333-364; terminal_type/is_terminal consistency enforced add_stage 392-403 / update_stage 507-516) are real but do not address the two open gaps: (1) no terminal_type→legacy-hired mapping in /move so custom hired stage yields no draft Contract; (2) clone_template still omits scorecard_schema."
 },
 {
  "id": "M4-P0.4",
  "verdict": "PARTIAL",
  "effort": "M",
  "evidence": "Both mechanisms named in the finding title are FIXED (all labeled \"M4 PR-02 (audyt P0.4)\", from PR #784). (1) Bypass-via-subsequent-move: backend/app/api/pipeline.py:381-392 — /move loads the current (latest, moved_at DESC/id DESC) row for the (candidate,job) pair and returns 409 if its verification_status==pending; bulk-move on verified/hired is also blocked with 422 at pipeline.py:1775-1783 (previously created verified rows with default verification_status=active, no rate). (2) Approve-on-stale-row: accept-verification (pipeline.py:1499-1515) and reject-verification (pipeline.py:1614-1630) recompute latest_id and 409 if candidate_stage_id != latest_id. RESIDUAL GAP reproducing the finding's impact: candidates.py:3079 set_recruitment_expected_rate (PATCH /candidates/{id}/recruitments/{job_id}/expected-rate) writes expected_rate_value onto the latest CandidateStage (3103-3125) and, per its own docstring (3099-3101), deliberately does NOT re-trigger the budget gate. It is callable by RECRUITMENT_RATE_EDIT_ROLES=(admin,delivery_lead,tac,recruiter) (recruitment_access.py:92-97) — includes recruiter — while approval needs ApproverPlus=(admin,delivery_lead,head_of_recruitment) (deps.py:228-237). So: move to verified within budget → active (no approval), then PATCH rate above budget → row stays verified/active with an over-budget rate and no approver sign-off (model default verification_status=active, recruitment_pipeline.py:148). This is exactly \"kandydat powyżej budżetu przechodzi dalej bez czyjejkolwiek zgody\".",
  "fix_hint": "In set_recruitment_expected_rate (candidates.py:3079), when the latest stage is `verified` (or already has budget_max_at_move set), re-run the normalize_rate_to_monthly comparison against job.salary_max / budget_max_at_move; if the new rate exceeds budget, set verification_status=pending, update budget_max_at_move, and call _notify_pending_verification — mirroring the /move gate. Also harden the /from-linkedin quick-assign path (_assign_candidate_to_job, candidates.py:2283) to reject stage=verified (and other rate-bearing/terminal stages) so a verified/active row cannot be created without going through the gate.",
  "confirm": "No guard closes the residual gap. The title's two mechanisms are fixed (pipeline.py:381-392 blocks move over a pending row; pipeline.py:1499-1515 & 1614-1630 recompute latest_id and 409 on stale accept/reject), but the impact is still reproducible via candidates.py:3078-3142 set_recruitment_expected_rate. That PATCH writes expected_rate_value directly (line 3123) with no normalize_rate_to_monthly, no salary_max compare, and no verification_status flip; its docstring (3099-3101) explicitly states it does NOT re-trigger the gate. Guarded only by RecruitmentRateEditAccess = (admin,delivery_lead,tac,recruiter) (recruitment_access.py:92-97) — recruiter-inclusive, not ApproverPlus. Schema ClientRateUpdate (schemas/pipeline.py:65) validates only ge=0. The budget gate fires solely at move-to-verified (pipeline.py:536-560); the pending-verification queue filters verification_status==pending (pipeline.py:1423), so an active row never surfaces for approval. No DB CHECK/trigger on candidate_stages links budget to expected_rate (only ck_candidate_stages_withdrawn_requires_reason, migration 0068; model recruitment_pipeline.py has no relevant __table_args__). Only two writers of expected_rate_value exist: gated /move (pipeline.py:581) and this un-gated PATCH (candidates.py:3123). Exploit: move to verified in-budget → active (no approval), then PATCH expected-rate above budget → verified/active row with over-budget rate and no approver sign-off."
 },
 {
  "id": "M5-P0.2",
  "verdict": "PARTIAL",
  "effort": "S",
  "evidence": "Two of three sub-claims are fixed, but the core \"not revocable\" gap remains. FIXED: (a) token hash-at-rest — mint_signature_link (backend/app/services/signing/sender.py:218-220) stores token=\"v2$<hex>\" non-secret PK + token_sha256=sha256(raw_token); secret only in URL (migration 0182, #846). (b) atomic single-use — submit_signed_pdf (backend/app/api/public_signing.py:158-174) runs UPDATE signature_links SET used_at WHERE token=... AND used_at IS NULL RETURNING token before finalize; loser → 410 (#844). STILL OPEN: withdraw_signature (backend/app/api/signing.py:204-233) only sets sig.status=withdrawn — it never revokes/expires the SignatureLink rows. SignatureLink.revoked is never set True anywhere (grep: only model default + the read at public_signing.py:60). regenerate_link (signing.py:240-260) mints a new link without revoking the old. And finalize_signed_pdf (sender.py:300-359) has no sig.status guard — it unconditionally sets status=completed and advances the candidate. So after a withdraw, an old link still passes _load_valid_link (public_signing.py:57-64: revoked=False, expires_at future, used_at NULL), the atomic claim succeeds, and finalize completes the signature — the cancellation is bypassed, exactly the finding's stated impact.",
  "fix_hint": "Two complementary changes: (1) On withdraw_signature and regenerate_link, revoke outstanding links: db.execute(update(SignatureLink).where(SignatureLink.signature_id==sig.id, SignatureLink.used_at.is_(None)).values(revoked=True)) before commit (regenerate: revoke prior links before minting the new one). (2) Fail-closed at consumption: in submit_signed_pdf/finalize_signed_pdf add a status guard rejecting when sig.status not in (SignatureStatus.sent, SignatureStatus.in_progress) — i.e. 409/410 for withdrawn/completed/expired/rejected — so a withdrawn signature can never be finalized even via a still-live token.",
  "confirm": "No guard exists — the \"not revocable\" sub-claim is genuinely still open (the token-hash and atomic-single-use sub-claims ARE fixed). Confirmed after a wide search: (1) finalize_signed_pdf has NO sig.status guard — backend/app/services/signing/sender.py:358 unconditionally sets sig.status=completed and advances the candidate, never rejecting a withdrawn signature. (2) submit_signed_pdf (backend/app/api/public_signing.py:140-176) only validates the LINK (revoked/expires_at/used_at) plus the atomic used_at claim; no sig.status check. (3) withdraw_signature (backend/app/api/signing.py:221) only sets sig.status=withdrawn and never touches SignatureLink rows — its docstring \"revokes the public link via expiry\" (signing.py:209) is not implemented; it changes neither link.revoked nor link.expires_at. (4) regenerate_link (backend/app/api/signing.py:257) mints a new link without revoking the old. (5) SignatureLink.revoked is set True NOWHERE in the backend (grep: only the model default at app/models/signature_link.py:66 and the read at public_signing.py:60). No ORM before_update/after_update listeners on DocumentSignature/SignatureLink; no DB trigger on signature_links (migration 0133 creates the table without one, 0182 is the token-hash migration); public_signing router is mounted plain (app/main.py:154/585+, only get_db + slowapi limiter, no status-checking dependency); origin/main is byte-identical to HEAD for all three files; no test asserts post-withdraw revocation. Net: after withdraw, an old link still has revoked=False, expires_at future, used_at NULL → _load_valid_link (public_signing.py:57-64) passes, the atomic claim at public_signing.py:158-172 succeeds, and finalize_signed_pdf completes the signature and advances the candidate — the cancellation is bypassed. Same for regenerate: the old link stays fully valid alongside the new one."
 },
 {
  "id": "M5-P0.8",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "backend/app/services/autenti/webhook_handler.py is unchanged since the original Autenti MVP (#102/#133); still live-wired (app/main.py:982 include_router, gated only by AUTENTI_ENABLED). The state transition in handle_event (lines 176-183) applies new_status whenever `new_status is not None and sig.status != new_status` — the ONLY guard is a no-op check, NOT a terminal/monotonic guard. `_intermediate_status_for_event` (lines 107-115) maps APPROVAL_PROCESS_CONSENTED / REVIEW_PROCESS_COMPLETED / HANDED_OVER → in_progress. Autenti webhooks are unordered, so a delayed intermediate event arriving after a terminal one downgrades it: a sig already at `completed` (or `rejected`) + a late HANDED_OVER event → `completed != in_progress` → `sig.status = in_progress`. No terminal-set / ordering check exists anywhere (grep for terminal/monotonic/revert/is_terminal in the handler, model, and route found none; tests only exercise the mapping helpers in isolation, no reversion test). Part 2 also reproduces: status flips to completed at lines 180-183 BEFORE `_dispatch_side_effects`→`_on_signing_completed` (line 187), where the signed-PDF download is best-effort in a try/except that only logs (lines 216-225), so `completed` is committed with signed_document_id=NULL when download fails (documented \"failure-tolerant\" behavior; the autenti_sweeper_loop only retries the download later, it does not prevent the completed-without-file window).",
  "fix_hint": "Add a monotonic guard in handle_event before assigning sig.status: define a TERMINAL set {completed, rejected, withdrawn, expired, failed} and refuse any transition when sig.status is already terminal (log + no-op), so late/out-of-order events cannot regress a settled signature. For part 2, only flip to SignatureStatus.completed after _download_and_attach_signed_file succeeds (or introduce an intermediate completed_pending_download state that the sweeper promotes once the PDF is attached), so a completed row always has signed_document_id set.",
  "confirm": "No guard exists — verified across handler, model, route, migrations, and git history. In backend/app/services/autenti/webhook_handler.py:180-183 the state transition applies `new_status` whenever `new_status is not None and sig.status != new_status`; this inequality is the ONLY guard, not a terminal/monotonic one. `_intermediate_status_for_event` (lines 107-115) maps HANDED_OVER/APPROVAL_PROCESS_CONSENTED/REVIEW_PROCESS_COMPLETED → in_progress, so a late HANDED_OVER after `completed`: `_terminal_status_for_event` returns None (line 104) → intermediate returns in_progress → completed != in_progress → sig.status downgraded to in_progress (Part 1 reversion, also applies to late terminal-vs-terminal like rejected after completed). Event-id dedup (156-173) only blocks exact same-event replays. Part 2: status set to completed at 180-183 BEFORE _dispatch_side_effects→_on_signing_completed (187/204) where the signed-PDF download is best-effort in a try/except that only logs (217-225), so `completed` commits with signed_document_id=NULL on download failure. Searched for a guard the verifier might have missed and found NONE: model document_signature.py:156-161 `status` is a plain mapped_column with no @validates/setter/CHECK on transitions; route app/api/autenti.py:318-344 calls handle_event directly with no terminal short-circuit; router is unconditionally included (main.py:982), gated only by AUTENTI_ENABLED (default False, config.py:550) so it fires when the kill-switch flips on; no migration adds a transition trigger/constraint; and the file's only recent commit (#506) was a trivial 4-line future-annotations edit, not a guard — logic unchanged since MVP #102/#133. The separate app/services/signing/ KIR rail does not wrap or protect the Autenti path."
 },
 {
  "id": "M5-P0.9",
  "verdict": "PARTIAL",
  "effort": "L",
  "evidence": "Send dispatch is still not restart-safe in either named file. Autenti: app/api/autenti.py:86 still does `asyncio.create_task(send_to_autenti(sig.id))` (fire-and-forget, unchanged since PR #102; later commit #839 only added TacPlus auth). send_to_autenti (app/services/autenti/sender.py:319-444) walks draft→sending→sent in its own session; a container restart between the 202 and completion orphans the row at draft/sending. NO task reconciles those states: autenti_expiry_sweeper._sweep_expired (l.66-76) and _retry_signed_downloads (l.138-144) only filter status in {sent,in_progress} / ==completed; signing_sweeper._expire_overdue (app/tasks/signing_sweeper.py:38-47) only filters {sent,in_progress}. grep for SignatureStatus.draft|sending across app/tasks/ and any startup routine = none; the only outbox in the codebase (index_outbox) is for search indexing. In-house: signing/sender.py prepare_and_send still commits twice — prepare_send commits status=draft (l.178), then prepare_and_send commits status=sent (l.294); a restart between them leaves a stray draft the sweeper never reconciles. Partial hardening exists but closes neither named failure mode across a restart: send_to_autenti uses idempotency_key=f\"sig-{sig.id}\" (l.377) and commits autenti_process_id in the same txn as the state move (l.395-396) — but a manual Resend creates a NEW row with a new key, so restart-after-create + resend still doubles the Autenti process. Blast radius is currently operational-only: AUTENTI_ENABLED and SIGNING_ENABLED both default False (config.py:550,585) and Autenti is \"OUT\" per M5 — flipping the switch makes the defect live.",
  "fix_hint": "Add a reconciliation sweep for DocumentSignature rows stuck in draft/sending past a short grace window (re-dispatch the same sig.id — which the existing idempotency_key makes safe — or fail them with a notification), so a restart mid-send self-heals instead of silently stalling. Collapse in-house prepare_and_send into a single transaction (drop the separate draft commit in prepare_send when called via prepare_and_send). Ideally replace asyncio.create_task(send_to_autenti) with a persisted outbox job (mirror app/tasks/index_outbox_worker.py) so the dispatch survives a restart; make Teams notification emit best-effort inside the committed txn rather than fire-and-forget.",
  "confirm": "No guard found — confirmed still open. Autenti dispatch is still fire-and-forget (app/api/autenti.py:86 asyncio.create_task(send_to_autenti)); send_to_autenti walks draft(sender.py:190)→sending(sender.py:358)→sent(sender.py:408) in its own session, so a restart orphans the row at draft/sending. The only guards present — the idempotency check `if sig.status != draft: return` (autenti/sender.py:342) and same-txn commit of autenti_process_id (sender.py:395-396) — prevent double-send within a live task but do NOT resume an orphan after restart (nothing re-invokes send_to_autenti). In-house prepare_and_send still double-commits: draft at signing/sender.py:178, sent at signing/sender.py:294. NO sweeper reconciles draft/sending: autenti_expiry_sweeper filters {sent,in_progress} (l.68-69) / ==completed (l.140-143); signing_sweeper filters {sent,in_progress} (l.41-44). grep SignatureStatus.draft|sending across app/ = only model default + the two senders, zero reconcilers. The lifespan has the exact reaper pattern needed but applies it ONLY to CvGeneratedDocument processing→failed (app/main.py:451-474), never to DocumentSignature; no signature reaper in startup, entrypoint.sh (only enum mirror l.144-153), CLI, or cron. The codebase itself documents the gap: app/api/admin_engagement_inventory.py:262-275 'stuck_signature' item says 'backlog wysyłki bez restart-safe recovery (P0.9)' — a read-only detection query, not a guard. Blast radius operational-only: AUTENTI_ENABLED/SIGNING_ENABLED both default False (config.py:550,585)."
 },
 {
  "id": "M5-P0.10",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "Two of the three named guards are absent; the third only incidentally mitigated. (1) SSRF: backend/app/services/autenti/pdf_renderer.py:99 renders `HTML(string=document, base_url=base_url).write_pdf()` with NO `url_fetcher`, so WeasyPrint's default fetcher (file://, http(s)://, data:) is active. Reachable with attacker-controlled HTML: app/services/signing/sender.py:195-202 (render_unsigned_pdf) feeds `sig.contract.draft_content_html` into it; that draft is saved VERBATIM by any TacPlus (tac=level 3, non-admin) user at contracts.py:1441; and it is exposed via the public endpoint app/api/public_signing.py:116 (GET /sign/{token}/pdf). A draft body with `<img src=\"http://169.254.169.254/…\">` or `file://` triggers a blind server-side fetch. CSP cannot help — WeasyPrint is server-side. (2) Jinja SSTI: backend/app/api/contract_templates.py:58 still uses plain `Environment(...)`, not `SandboxedEnvironment` — while the sibling app/api/user_email_templates.py:45 does use SandboxedEnvironment (team knows the pattern, not applied here). (3) Stored XSS: contracts.py:1494 / contract_templates.py:351 serve raw draft HTML via HTMLResponse with no sanitization; only incidental mitigation is a blanket global CSP `default-src 'none'` at main.py:297-300 set via setdefault on non-DEBUG responses (comment says \"we don't serve HTML here\"), which blocks script exec in prod but is not a per-route trust boundary and is off in DEBUG.",
  "fix_hint": "Three changes: (a) SSRF — pass a deny-all `url_fetcher` to WeasyPrint in pdf_renderer.py that rejects everything except embedded `data:` URIs (raise on http/https/file/ftp); (b) Jinja — swap `Environment` for `jinja2.sandbox.SandboxedEnvironment` in contract_templates.py:58 (mirror user_email_templates.py); (c) XSS — run draft_content_html through the existing allowlist sanitizer (app/services/html_sanitizer.sanitize_cv_html, already used in public_share.py / candidate_stage_cv.py) on save at contracts.py:1441 and/or set an explicit restrictive CSP on the HTML-serving routes rather than relying on the incidental global one.",
  "confirm": "No guard closes this finding — the verifier's core claims are confirmed by direct reading + repo-wide grep. (1) SSRF: backend/app/services/autenti/pdf_renderer.py:99 renders `HTML(string=document, base_url=base_url).write_pdf()` with NO `url_fetcher`; grep for `url_fetcher`/`default_url_fetcher` across the whole repo returns zero hits, so WeasyPrint's default fetcher (file://, http(s)://, data:) is active. WeasyPrint is genuinely installed in prod (requirements.txt:45 `weasyprint==68.0`; Dockerfile:15-17 libpango/cairo). Reachable via the public unauthenticated endpoint app/api/public_signing.py:116 → render_unsigned_pdf (sender.py:202, importing the autenti renderer through the signing re-export at signing/pdf_renderer.py:14) → `sig.contract.draft_content_html`, which is saved VERBATIM (no sanitization) by non-admin TacPlus users at contracts.py:1442. The only guard I could find is the feature-flag kill-switch SIGNING_ENABLED=False / AUTENTI_ENABLED=False (config.py:585/550) enforced by _require_enabled() in sender.prepare_send — this makes the path DORMANT but is not a trust boundary; once the signing rail (which exists to be enabled) is on, a TacPlus user's `<img src=http://169.254.169.254/...>` triggers a blind server-side fetch. (2) SSTI: contract_templates.py:58 still uses plain `Environment(...)`, not SandboxedEnvironment — the sibling user_email_templates.py:45 uses SandboxedEnvironment, so the pattern is known but unapplied here; autoescape does not stop SSTI. Template writes are AdminUser-gated (create_template/update_template), lowering severity to admin-only but not restoring the sandbox. (3) Stored XSS: contract_templates.py:351 / contracts.py render_draft_for_print serve raw HTML unsanitized; the only mitigation is the global CSP `default-src 'none'` at main.py:297-300 set via setdefault only when not settings.DEBUG (off in DEBUG, not per-route). A bleach html_sanitizer.py exists and is wired into the CV rails (candidate_stage_cv.py:399, public_share.py:223) but is NOT applied to the contract draft rail. No shared dependency, middleware, decorator, entrypoint mirror, service-layer check, or recent commit adds a deny-all fetcher or a Jinja sandbox to this path."
 },
 {
  "id": "M6-P0.11",
  "verdict": "PARTIAL",
  "effort": "S",
  "evidence": "Three of the four sub-claims are now closed, one remains. FIXED: (a) transcript/recording reads — backend/app/api/calls.py:78 list_candidate_calls now uses CandidatePIIAccess and cloudtalk.py:295 initiate_call uses CandidatePIIAccess (require_roles over admin/head_of_recruitment/delivery_lead/tac/recruiter/sourcer; viewer `user` excluded per candidate_access.py:50-59,168); fireflies.py:37 list_fireflies_transcripts now uses OperationalUser (deps.py:210-222, viewer excluded). (b) CloudTalk agent mapping — cloudtalk.py list_cloudtalk_agents:126, assign_agent_to_user:162, unassign_agent:197, sync_agents:213 all now require AdminUser (deps.py:173), fixed by #833. STILL OPEN: backend/app/api/fireflies.py:22-32 trigger_fireflies_sync still takes bare `current_user: CurrentUser` (line 24) — any authenticated user including the read-only viewer/client `user` role — and it is a GET that mutates (calls sync_fireflies_transcripts(db), which writes meeting notes and hits the external Fireflies API). Router mounted with no dependency (main.py:867). Also fireflies.py:71 fireflies_status is CurrentUser (aggregate-only, low concern). None of the endpoints do object-level/ownership checks, but the containment model is role-based and the PII reads are now correctly role-gated; the real residual is the viewer-triggerable mutating GET sync.",
  "fix_hint": "Change `current_user: CurrentUser` to `current_user: OperationalUser` on trigger_fireflies_sync (fireflies.py:24) so the read-only viewer `user` role cannot trigger the global mutating sync; consider POST instead of GET for the mutation, and gate fireflies_status:71 to OperationalUser too.",
  "confirm": "Could not refute — no guard closes the residual. backend/app/api/fireflies.py:24 trigger_fireflies_sync takes bare `current_user: CurrentUser` (CurrentUser = Depends(get_current_user), deps.py:171/130-133 — only validates JWT + is_active, so viewer/client UserRole.user passes). It is a GET that mutates: sync_fireflies_transcripts(db) (fireflies_sync.py:147) writes Note rows (db.add/db.commit lines 251/286), calls the external Fireflies API, and fires LLM champion-draft enrichment (line 296); the service does no role check (only FIREFLIES_API_KEY guard, line 154). Router mounted with no dependency (main.py:867). No path/role middleware (main.py has only SecurityHeaders, LegacyStatsDeprecation=dynareporter-only, CORS). No FIREFLIES_ENABLED kill-switch. No in-body authz check in the handler. The endpoint is explicitly registered as a known-bare hole in the frozen baseline at tests/test_route_authz_contract.py:248 (a detection/freeze mechanism, not a guard). Git PR #840 (56dc2b81) gated only list_fireflies_transcripts→OperationalUser and deliberately left trigger_fireflies_sync + fireflies_status on bare CurrentUser — a partial fix. The sibling sub-claims the verifier marked FIXED are indeed closed (calls.py:78 CandidatePIIAccess, cloudtalk.py:295 CandidatePIIAccess, cloudtalk agent-mapping AdminUser via #833, fireflies transcripts OperationalUser), but the mutating viewer-triggerable GET sync remains open."
 },
 {
  "id": "M6-P0.12",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "backend/app/api/calls.py:465-501 — `POST /calls/webhook/{token}` puts the raw signing secret in the URL path: line 497 does `_hmac.compare_digest(token, settings.CLOUDTALK_WEBHOOK_SECRET)`, i.e. the URL token IS the HMAC secret. The docstring (lines 478-480) itself admits \"FastAPI access logs strip path query string but NOT path segments,\" so the secret leaks to Cloudflare/Traefik/access logs. backend/app/services/cloudtalk/webhook_verify.py:33 — `verify_signature` HMACs `body` only; no timestamp/freshness/nonce. Comment at lines 8-10 defers replay protection to the `cloudtalk_call_id` UNIQUE constraint, which only dedups inserts — it does not reject a replayed signed request, and each replay re-runs `enrich_from_call` (calls.py:432-455). Neither guard exists. M6 Fala A (#805, f00cce27) touched calls.py only for iCal SSRF/calendar, not these webhook paths; webhook_verify.py unchanged since #157.",
  "fix_hint": "(1) Remove the `/calls/webhook/{token}` URL-secret variant, or replace it with a distinct opaque token stored hashed-at-rest (SHA-256) that is NOT the HMAC signing secret, and never log the request path for that route. (2) Add replay protection: require an `X-CloudTalk-Timestamp` header, fold it into the signed material (`hmac(secret, f\"{timestamp}.{body}\")`), reject requests whose timestamp is outside a small window (e.g. ±5 min), and optionally keep a short-lived seen-signature/nonce cache. Make enrichment idempotent on repeated deliveries of the same ct_id.",
  "confirm": "No guard closes it. (1) Secret-in-URL: backend/app/api/calls.py:465-501 still exposes POST /calls/webhook/{token} (mounted /api, main.py:733, no auth dep); line 497 compares the URL path segment directly against settings.CLOUDTALK_WEBHOOK_SECRET — the path IS the secret. (2) Replay: backend/app/services/cloudtalk/webhook_verify.py:19-34 HMACs body only, no timestamp/nonce/freshness; comment lines 8-10 defer to the cloudtalk_call_id UNIQUE constraint which only dedups INSERTs — a replayed signed body still hits the update branch (calls.py:407-426) and re-runs enrich_from_call every time (calls.py:432-455). No X-CloudTalk-Timestamp handling exists and config.py:638-658 has no CloudTalk timestamp/nonce setting (contrast Autenti's iat replay defense at config.py:570-573, a different webhook). Controls that do NOT close it: kill-switch CLOUDTALK_ENABLED=false (config.py:638) is dormant deployment state, not a fix — documented activation flips it true; #843 log-masking (logging_config.py:47-66) only redacts the app's uvicorn.access log (attached logging_config.py:112), not Traefik/Cloudflare access logs, does not remove the secret from the URL, and does nothing for replay; #805 (f00cce27) only added RBAC to the human-facing endpoints (calls.py:131,149), not the webhook."
 },
 {
  "id": "M7-P0.4",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "Finance endpoints accept a period but compute as-of-today and label the envelope with the requested period. backend/app/api/analytics_v1.py: get_finance_summary (L413-430) calls metrics.finance_summary(db) with NO period; get_finance_clients (L458-475) calls finance_clients(db); get_client_finance (L377-397) calls client_finance(db, client_id); get_executive_board (L478-498) calls finance_summary(db). All build the envelope via _cached_envelope with period=period.as_payload() (L143, unconditional). In backend/app/analytics/metrics.py, finance_summary(db) (L560, docstring \"date-effective na dziś\"), _active_contracts (L420) hardcodes today=date.today() (L421) with no as-of param, and _sum_finance defaults on=date.today() (L477) — the summary/clients/client paths never override it. Result: request for Jan 2025 returns 2026-07-21 figures stamped period.start=2025-01-01/end=2025-02-01. No guard: grep for as-of/period warnings finds only the L563 docstring; _finance_quality (analytics_v1.py:400) surfaces only FX warnings, never a period-mismatch warning. No middleware/dependency/entrypoint mirror corrects it.",
  "fix_hint": "Thread the period's effective as-of date into the point-in-time finance path: add an `on: date` param to `_active_contracts` (replace hardcoded date.today() with it), give finance_summary/finance_clients/client_finance a period/as-of argument, and pass `min(period.end, today)` (or period.end capped) from the four endpoints, forwarding `on=` into `_sum_finance`. Alternatively, if finance is intentionally always current, drop the `period` query param from these endpoints (or emit a quality warning stating the data is as-of-today) so the envelope stops mislabeling historical requests.",
  "confirm": "No guard exists. In backend/app/api/analytics_v1.py the four finance endpoints call the metrics with NO period yet stamp the envelope with the requested period unconditionally: get_finance_summary L421 (finance_summary(db)) + envelope period=period via _cached_envelope L428→L143; get_finance_clients L466 (finance_clients(db)) L473; get_client_finance L388 (client_finance(db, client_id)) L395; get_executive_board L488 (finance_summary(db)) L495. In backend/app/analytics/metrics.py finance_summary (L560), finance_clients (L683) and client_finance (L767) take no period; _active_contracts hardcodes today=date.today() (L421) and _sum_finance defaults on=date.today() (L477) — these three paths never override on (only finance_trend passes on=month_start L665, and it is not flagged). A past-labelled mismatch is reachable because _parse_period (L79) accepts period=custom with arbitrary past date_from/date_to (periods.py L105-120), while standard kinds always resolve to the current period (periods.py L122-156). No warning/rejection anywhere: build_envelope (schemas.py L64-82) labels with whatever period is passed; _finance_quality (analytics_v1.py L400-407) surfaces only FX warnings, never a period mismatch; grep for snapshot/as-of finds only the L563 docstring 'date-effective na dziś'; the v1 router is mounted with prefix only (main.py L913) and the sole middleware are SecurityHeaders + LegacyStatsDeprecation (L563-564), neither touching the period; no period.kind/custom inspection exists in the finance path; finance tests assert no period-ignored warning or 422. Git status clean, so this matches HEAD."
 },
 {
  "id": "M7-P0.8",
  "verdict": "OPEN",
  "effort": "S",
  "evidence": "The canonical milestone source `analytics_first_milestones` counts every `candidate_stages` row with `stage='verified'` regardless of `verification_status`. Current view def: `backend/alembic/versions/0175_analytics_milestones_acceptance.py:48` and its safety-net mirror `backend/entrypoint.sh:1452` filter ONLY `WHERE cs.stage IN ('verified', 'cv_sent', ...)` — no `verification_status` predicate. A `pending`/`rejected` verified move persists as a `stage='verified'` row: `backend/app/api/pipeline.py:559,570-591` inserts stage=verified + verification_status=pending immediately (before any approval); reject at `backend/app/api/pipeline.py:1634` keeps the row at `stage='verified'` and only flips status→rejected for audit. Both named files consume the view unfiltered: `backend/app/analytics/metrics.py:266` (user_kpis `first_verifications`), `:349` (team_kpis), `:165` (recruitment_funnel); `backend/app/services/kpi_engine.py:290-305` (count_canonical_metric via VERIFIER_ANCHORED_CTE). The credit-theft half also reproduces: `backend/app/services/kpi_panel.py:98-108` anchor CTE sets `verifier = first_moved_by` of the first `stage='verified'` row (rn=1) and `credited.credit_user = COALESCE(a.verifier, mf.first_mover)`, so a pending/rejected verifier becomes the anchor and is credited for all later milestones (cv_sent/interview/hired) even if re-verified and driven to hire by someone else. No middleware/dependency/later migration adds the missing filter.",
  "fix_hint": "Restrict the `verified` rows in the `analytics_first_milestones` view to accepted verifications, e.g. add `AND (cs.stage <> 'verified' OR cs.verification_status = 'active')` (or globally `AND cs.verification_status = 'active'`, since non-verified moves default to 'active') to the WHERE clause. Apply as a `CREATE OR REPLACE VIEW` migration AND mirror it in `backend/entrypoint.sh` (_VIEW/_COLUMN statements) to survive the orphaned-alembic safety net. Add a KPI parity test that a pending/rejected verified stage does not count toward first_verifications and does not anchor downstream milestone credit.",
  "confirm": "No guard exists — genuinely open. The canonical view `analytics_first_milestones` filters only on stage with no verification_status predicate (migration `backend/alembic/versions/0175_analytics_milestones_acceptance.py:48`; mirror `backend/entrypoint.sh:1453`), and only migrations 0174/0175 ever define it — 0176–0183 do not add a filter. The reject path `backend/app/api/pipeline.py:1634` flips verification_status→rejected but keeps the stage='verified' row (create path pipeline.py:559,580-590 inserts stage=verified+pending pre-approval), so pending/rejected verified moves persist as canonical rows. The anchor CTE `backend/app/services/kpi_panel.py:96-107` reads the view unfiltered and sets credit_user=COALESCE(verifier, first_mover) off the earliest verified row, and every consumer (metrics.py:119/157/165/243/266/349, kpi_engine.py:290-305) reads it unfiltered. A grep for verification_status/rejected/pending across all analytics/kpi/reports/dashboard consumers returns zero matches; no trigger exists on candidate_stages; and test_analytics_views.py:140-143 asserts the first-verifier-anchored behavior as intended. Both halves (pending/rejected counted as success + credit theft of later milestones) reproduce with no closing guard."
 },
 {
  "id": "M7-P0.11",
  "verdict": "OPEN",
  "effort": "M",
  "evidence": "Routes live: app/main.py:692 mounts contract_analytics at /api/contract-analytics; Contract.currency exists (models/contract.py:134). (1) margin_by_contractor (contract_analytics.py:99-113) and margin_by_client (139-152) do func.sum(marg_sql)/func.sum(rev_sql) grouped only by candidate/client — no currency grouping or conversion, so EUR+PLN are added nominally into one int. (2) revenue_forecast sums _monthly_rate_client/_monthly_margin (270-274) and its convert_currency flag DEFAULTS to False (230-233), so the default response also sums currencies nominally. (3) DTOs still use int: MarginByContractor 53-54, MarginByClient 62-63, ForecastMonth 78-79. (4) fx_service.convert_to_pln:105-107 silently falls back to 1:1 (returns Decimal(amount)) when no rate is cached. git log: file reverted to 2026-07-13 by #713; #697 only hardened RBAC (DeliveryLeadPlus dep), left FX/summing untouched.",
  "fix_hint": "In margin_by_contractor/margin_by_client, add Contract.currency to the GROUP BY (or fetch per-currency subtotals) and convert each subtotal to PLN via convert_to_pln before summing; in revenue_forecast normalize unconditionally (drop the convert_currency=False default); change DTO amount fields from int to Decimal; and replace the silent 1:1 fallback in convert_to_pln (lines 105-107) with a hard error or a degraded/rate-missing flag so a missing NBP rate cannot masquerade as a correct total.",
  "confirm": "No guard exists at any layer; every sub-claim confirmed in current code. Routing live & unguarded: app/main.py:691-695 mounts contract_analytics at /api/contract-analytics with only DeliveryLeadPlus RBAC (the #697 hardening), no FX/currency guard. (1) margin_by_contractor sums func.sum(marg_sql)/func.sum(rev_sql) at contract_analytics.py:105-106 grouped only by candidate (group_by :110); margin_by_client at :144-145 grouped only by client (:149) — no Contract.currency in group_by, no conversion, so EUR+PLN are summed nominally. (2) revenue_forecast convert_currency defaults False (:230-233); _to_display (:247-251) returns raw amount when not convert_currency; sums at :273-274 — default response sums currencies nominally. (3) int DTOs: MarginByContractor :53-54, MarginByClient :62-63, ForecastMonth :78-79. (4) fx_service.convert_to_pln:105-107 returns Decimal(amount) unchanged (1:1) when no cached rate. Ruled-out guards: Contract.currency is free String(3) default PLN (models/contract.py:134), migration 0037:272 VARCHAR(3) NOT NULL DEFAULT 'PLN' with NO CHECK constraint (no migration adds one); schema currency: str = \"PLN\" free-form with no validator (schemas/contract.py:99) so non-PLN is reachable via API; reports._monthly_rate_client/_monthly_margin (reports.py:52-57) do no conversion; git log shows newest commit = #713 revert (d507d957), #697 only RBAC, fx_service untouched since #37; tests/test_contract_analytics.py only asserts response shape, no currency-correctness guard."
 },
 {
  "id": "M1B-SEC-01",
  "verdict": "OPEN",
  "effort": "S",
  "evidence": "backend/app/api/prep_kit.py:56-99 — POST /api/prep-kit/generate fetches ClientKnowledge filtered only by job.client_id derived from the caller-supplied request.job_id (lines 95-99), with NO per-client access check. The response leaks that knowledge: client_overview (tech_stack/culture/general, lines 132-158), selling_points (lines 266-268), and likely_questions (Tier 3 of suggest_questions_for_prep pulls ClientKnowledge category interview_questions for job.client_id, question_suggestions.py:327-333). The only change since the audit is commit 9e10ac82 (#833), which swapped get_current_user -> CandidatePIIAccess — a pure role gate (require_roles over admin/HoR/DL/TAC/recruiter/sourcer, candidate_access.py:168) that excludes the read-only `user` viewer but admits every recruiter/sourcer. The finding's threat actor (\"Rekruter przypisany tylko do klienta A\") is a recruiter, which passes that gate, so the cross-team BOLA reproduces unchanged. No ClientAccess dependency, resolve_client_access, or can_view_knowledge call exists anywhere on this path (grep shows only bare job.client_id refs); router is mounted at main.py:853 with no router-level dependency; no middleware enforces client containment. Contrast the sibling GET client_knowledge.py:62-65 which correctly gates with assert_client_exists + resolve_client_access + access.can_view_knowledge — the guard prep_kit is missing.",
  "fix_hint": "After loading the Job (and confirming job.client_id), mirror client_knowledge.py before fetching ClientKnowledge: `await assert_client_exists(db, job.client_id); access = await resolve_client_access(db, current_user, job.client_id); if not access.can_view_knowledge: raise deny(\"brak dostępu do wiedzy tego klienta\")`. This gates client_overview, selling_points, and the Tier-3 interview questions behind the same per-client containment already enforced on GET /clients/{id}/knowledge. Keep the existing CandidatePIIAccess role gate for the candidate-side data.",
  "confirm": "No guard exists — finding is OPEN. POST /api/prep-kit/generate (backend/app/api/prep_kit.py:56-99) fetches Job by caller-supplied request.job_id and loads ALL ClientKnowledge for job.client_id (lines 94-99) with no per-client check, leaking it via client_overview (lines 132-158), selling_points (lines 266-268) and likely_questions Tier 3 (app/services/question_suggestions.py:326-343). The only auth is CandidatePIIAccess = require_roles(*CANDIDATE_READ_ROLES) (backend/app/api/candidate_access.py:168, roles incl. recruiter/sourcer at :50-59); require_roles (app/api/deps.py) does only has_any_role with zero client scoping, so the threat actor (recruiter for client A) passes. Router mounted at app/main.py:853 with no dependencies=; only SecurityHeaders/LegacyStats/CORS middleware (main.py:563-566) — none enforce client containment. prep_kit.py and question_suggestions.py contain no resolve_client_access/assert_client_exists/can_view_knowledge/ClientAccess call, unlike the correctly-gated sibling GET at app/api/client_knowledge.py:62-65. Latest commit on prep_kit.py is #833 (9e10ac82), the role-gate swap itself; no later fix."
 }
]