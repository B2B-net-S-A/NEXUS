fastapi==0.141.1
uvicorn[standard]==0.52.4
# The `[asyncio]` extra is LOAD-BEARING from Python 3.13 onwards, do not drop it.
# SQLAlchemy 2.0.36 declares its greenlet dependency as
#   `greenlet!=0.4.17; python_version < "3.13" and (platform_machine == ...)`
# so on 3.13 a bare `sqlalchemy==2.0.36` installs NO greenlet at all. The image
# still builds and `import app.main` still succeeds — it only blows up at the
# first async DB call with "the greenlet library is required to use this
# function", i.e. every request. The `asyncio` extra declares greenlet without
# the python_version marker, which is the whole point of using it here.
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
pydantic[email]==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
httpx==0.28.1
# PIN AWARYJNY — NIE podnosić bez migracji API. qdrant-client 1.18.0 USUNĄŁ
# `QdrantClient.search()` / `AsyncQdrantClient.search()` na rzecz
# `query_points()` (zwraca obiekt z `.points`, nie listę). Podbicie w #961
# przeszło CI i healthcheck, bo żadne z nich nie dotyka ścieżki wektorowej, a
# wyjątek jest połykany do pustej listy — wyszukiwanie semantyczne i matching
# zwracały „brak wyników" zamiast błędu, przy `status: healthy`.
# Wołających jest 7: embedding_service.py (×4), pool_suggester.py,
# historical_jobs_retrieval.py, cc_classifier.py. Migracja na query_points —
# osobny PR, razem z sondą `checks.qdrant` w /api/health.
qdrant-client==1.12.1
# Dependabot #1169 (zmergowany 21.08) podniósł tu DOLNĄ granicę zakresu do
# >=0.122.0. Pin 0.125.0 mieści się w niej i jest ściśle mocniejszy, więc
# zastępuje zakres zamiast się z nim ścierać.
# PIN DOKŁADNY, nie zakres. `>=0.121.0,<1.0` był ostatnią pływającą zależnością
# BEZPOŚREDNIĄ, a Coolify przebudowuje obraz ze źródeł przy KAŻDYM deployu ORAZ
# przy każdym rollbacku (Deployments -> poprzedni -> Redeploy klonuje repo i
# odpala pip od nowa). Skutki, które to realnie psuło:
#  - rollback cofał kod aplikacji, ale NIE jej zależności — regresja wywołana
#    bumpem biblioteki wracała wiernie zainstalowana;
#  - CI zielenił się na jednym rozwiązaniu zakresu, a prod startował z innym,
#    rozwiązanym w innej chwili;
#  - bump lądował na prodzie bez ani jednego diffa i bez review.
# To nie jest hipoteza: ta biblioteka leży na ścieżce generowania CV, uzasadnień
# dopasowania i chatu w interaktywnym CV, a ścieżki AI są celowo fail-open —
# objawem nie byłoby 500, tylko po cichu pusty wynik. Dokładnie ten scenariusz
# jest już udokumentowany wyżej przy qdrant-client (przeszło CI i healthcheck,
# a wyszukiwanie semantyczne zwracało pustkę).
# 0.125.0 = wersja, którą stary zakres rozwiązywał w chwili przypięcia
# (2026-08-21), więc to zamrożenie stanu bieżącego, nie podbicie. Uwaga: 1.0.0
# JUŻ istnieje na PyPI — górna granica `<1.0` była jedyną rzeczą, która trzymała
# prod przed wskoczeniem w major. Podnoszenie = świadomy PR z lekturą release
# notes, nie efekt uboczny deployu.
anthropic==0.125.0
# pdfminer.six + pdfplumber to PARA SPRZĘŻONA — pdfplumber pinuje pdfminer.six
# dokładnym `==`, więc obie linie muszą się ruszać razem. Mapowanie pinów:
#   pdfplumber 0.11.4/0.11.5 -> 20231228 | 0.11.6 -> 20250327 | 0.11.7 -> 20250506
#   pdfplumber 0.11.8        -> 20251107 | 0.11.9 -> 20251230
#   pdfplumber 0.11.10       -> 20260107 (najnowsze)
# Oba zawierają fix CVE-2025-64512 (RCE przez pickle w składnicy CMap), który
# wszedł w 20251230. Wcześniejszy komentarz twierdził, że 20260107 jest
# nieosiągalny, bo żaden pdfplumber go nie pinuje — 0.11.10 to unieważnił.
pdfminer.six==20260107
pdfplumber==0.11.10
pytesseract==0.3.13
pdf2image==1.17.0
python-docx==1.2.0
# Generator Umów B2B — wypełnianie szablonu .docx placeholderami Jinja
# (na bazie python-docx; zachowuje formatowanie prawne umowy).
docxtpl==0.20.2
python-dotenv==1.2.3
python-multipart==0.0.32
aiofiles==25.1.0
jinja2==3.1.6
psycopg2-binary==2.9.12
openpyxl==3.1.5
icalendar==7.3.0
python-json-logger==4.2.0
# Security & observability
slowapi==0.1.10
sentry-sdk[fastapi]==2.68.0
# Microsoft 365 integration (Phase M365.1)
# msal>=1.34 required to allow cryptography>=48 (older msal capped cryptography<46).
msal==1.37.0
cryptography==50.0.0
bleach==6.4.0
# Proxycurl LinkedIn tracking — fuzzy company-name matching for diff detection.
rapidfuzz==3.14.5
# Autenti e-signature integration — server-side HTML→PDF for contract drafts
# before upload. python-jose already pinned above (used by JWT auth + webhook
# JWKS verification).
weasyprint==69.0
# In-house QES rail — PAdES validation (Option B upload-and-validate, zero KIR).
# 0.36.2 requires cryptography>=47 (no upper bound — zgodny z pinem 50.x wyżej).
# [etsi] adds EU Trusted List parsing (xsdata, signxml, lxml). Authoritative
# QES verdict via EU DSS sidecar; pyHanko is the local integrity pre-check.
# See docs/in-house-qes-signature-plan.md.
pyhanko[etsi]==0.36.2

# Hetzner Object Storage (S3-compatible) — CV blob migration (audit-2026-05-07 Faza 3).
boto3==1.43.77

# Polski kalendarz świąt dla `app.core.scheduling.is_business_day`. Targety
# Priority Work ("4 weryfikacje / dzień roboczy") i triggery notyfikacji nie mogą
# liczyć 1 maja ani Bożego Ciała jako dnia pracy. Ruchome święta liczone z daty
# Wielkanocy — dlatego biblioteka, nie własna tablica dat.
holidays==0.103
